# Offene Punkte

## Diese Woche

### Telegram-Bot fertig einrichten

Bot-Code ist fertig (Google Apps Script, kein API-Key nötig, kostenlos).
Bot-Token ist gespeichert (in Notizen-App).

Noch offen – nächste Sitzung hier weitermachen:

1. script.google.com im Browser öffnen → gespeichertes Projekt öffnen
2. Alten Code ersetzen: Strg+A → Entf → neuen Code einfügen → Strg+S
3. Bot-Token in Zeile 2 eintragen (aus Notizen kopieren)
4. Auf Telegram: eigenen Bot suchen → /start schreiben
5. In Google Script: Funktion `meineChatIdFinden` ausführen → Chat-ID notieren
6. Chat-ID in Zeile 3 eintragen → Strg+S
7. Funktion `jetztSenden` testen → prüfen ob Nachricht auf Telegram ankommt
8. Automatischen Trigger einrichten (jeden Samstag 12:00)

## Demnächst

- Content aus den Zielgruppen-Fragen entwickeln (Posts, Reels, Newsletter)

## Ideen / später

- Fragenbank erweitern oder durch KI-generierte Fragen ersetzen
